[project]
name = "clinical-nlp-claims-processing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alexr", email = "alexretana@example.com" }
]
requires-python = ">=3.12"
dependencies = [
    "jupyter-ai==3.0.0b7",
    "jupyterlab>=4.0.0",
]

[dependency-groups]
dev = [
    "ipykernel",
]

[build-system]
requires = ["uv_build>=0.9.2,<0.10.0"]
build-backend = "uv_build"
